create table Pessoa (
	id int,
	nome varchar(50),
	idade int,
	primary key(id)
);

create table Emprego (
	id int,
	nome varchar(50),
	idemprego int,
	primary key(id)
);

create table LocalEmpresa (
	idemprego int,
	local varchar(50),
	primary key(idemprego)
);
